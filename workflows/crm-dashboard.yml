name: CRM Dashboard Check
vars:
  customer: acme
steps:
  - action: open
    url: https://crm.example.com
  - action: fill
    selector: input[name=customer]
    value: "{{customer}}"
  - action: wait-for
    selector: main
  - action: screenshot
    path: crm-dashboard.png
